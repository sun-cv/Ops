


# Security Rules

These are hard rules — no exceptions:

- Never commit, suggest, or output secrets, API keys, tokens, or credentials
- Never modify files outside the current active project directory
- Never touch infrastructure or deployment configs unless I explicitly ask
- If a task would require crossing any of these lines, stop and flag it — do not work around it
- Point out if something I'm asking could accidentally expose secrets or violate these rules

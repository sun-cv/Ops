/Backups
/Logs
/editors/vscode/

# Claude config: runtime state and secrets are handled by ai/claude/.gitignore
# (a fail-safe allowlist). The live config dir is a real ~/.claude outside this repo.
# See ai/claude/README.md for the source-vs-runtime model.
